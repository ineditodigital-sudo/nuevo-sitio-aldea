# URL base de la API PHP.
# En desarrollo, si lo dejas vacío, el sitio usa el contenido local (fallback).
# En producción (cPanel) suele ser el mismo dominio:
# VITE_API_URL=https://aldea.work
VITE_API_URL=
